# rs70archive

Static photo archive with auto-generated WebP assets and sitemap.

## Quickstart

- `npm install`
- `npm run convert:webp`
- `npm run build`

## Notes

- WebP conversion writes `.webp` files next to originals.
- Set `WEBP_PROCESS_CR2=1` to include `.CR2` files.
